module.exports = {
    API: {
        providers: [
            {
                key: "localhost",
                endpoint: 'http://127.0.0.1:8000/',
                token_prefix: 'JWT '
            }
        ]
    },
};